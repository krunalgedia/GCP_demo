name: Testing Variables defined inside workflow file

on:
  workflow_dispatch

env:
  cloud: google-cloud

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: "Heyyy!"
        run: echo "$Greeting $First_Name is using $cloud!"
        env:
          First_Name: Krunal